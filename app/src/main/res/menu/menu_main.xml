<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/pref_view_uncompleted"
        android:checked="false"
        android:title="@string/_uncompleted_only"
        app:showAsAction="never" />
    <item
        android:id="@+id/pref_view_completed"
        android:title="@string/_completed_only"
        app:showAsAction="never" />
    <item
        android:id="@+id/pref_view_all"
        android:title="@string/_show_all"
        app:showAsAction="never" />
</menu>